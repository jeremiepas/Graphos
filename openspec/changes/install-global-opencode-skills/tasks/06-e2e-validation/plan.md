# Task 6 — End-to-end validation and PDCA close-out — PLAN

**Task slug**: `06-e2e-validation`
**Attempt**: 1
**Status**: pending

## Summary

Run the full validation suite, perform a manual smoke test of the generated files, and close the PDCA cycle for the change.

## Detail

### Scope
- Run `cabal build --flag dev` and confirm zero warnings.
- Run `cabal test` and confirm all tests pass.
- Manually run `graphos install-skill --target opencode` (or its test equivalent) and inspect generated files.
- Verify idempotency by running the command twice.
- Record results and decide next steps (archive, follow-up cycle).

### Check Criteria (defined before code)
- C1: `cabal build --flag dev` exits 0 with zero warnings.
- C2: `cabal test` exits 0.
- C3: Generated skill files start with `---\nname: ...\ndescription: ...\n---\n` followed by `generated by graphos ...`.
- C4: Second invocation skips existing files and exits 0.
- C5: `graphos-query` skill body forbids build/update/ingest.

### Affected Modules
- None (validation task).

### Prerequisites
- All prior tasks completed.

### Risks
- Manual smoke test could touch the real home directory. Mitigation: use a temporary `HOME` or the injectable test path if a direct CLI smoke test is unsafe.

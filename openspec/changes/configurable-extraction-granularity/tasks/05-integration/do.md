# Do: Integration verification + CHANGELOG

- Full pipeline runs: default (`function`) and `--granularity fine` (CLI override confirmed in logs).
- Collected per-file node counts for the tree-sitter-extracted TS example files.
- Wrote the CHANGELOG "Unreleased" section covering the granularity default change (with rollback line) plus the companion fixes shipped this cycle.

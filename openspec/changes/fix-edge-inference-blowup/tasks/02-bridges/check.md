# Check
- cabal test PASS. Attempt history: triangle fixture failed (connecting-edge endpoints were the centroids;
  notEdgeAlready correctly suppressed the bridge) — fixture redesigned, not the implementation.

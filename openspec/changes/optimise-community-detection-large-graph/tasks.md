<!--
  PDCA-PER-TASK workflow.
  Each top-level `## N. <task>` is ONE task = ONE complete PDCA micro-cycle.
  Within the task, run the steps in order and keep checkbox format so progress
  can be tracked: `- [ ] N.P …`, `- [ ] N.D …`, `- [ ] N.C …`, `- [ ] N.A …`.

  PASS rule:  a task PASSES only when its Check passes AND its Act is OK.
              A passed task reaches the same done state as in classic SDD.
  RETRY rule: if Act is NOT OK, the task does NOT pass — record the failed
              attempt under "### Attempt history (N)" (KEEP THE TRACE, never
              delete it), then start a NEW P → D → C → A attempt for the same
              task. Repeat until an attempt passes.

  Everything else matches the official spec-driven workflow.
-->

## 1. Unstub Pipeline.hs Step 5 (re-cluster + analyze + aggregates)

- [x] 1.P Plan: In `src/Graphos/UseCase/Pipeline.hs` lines 263-264, replace the stub `(Map.empty, Map.empty)` and `Analysis Map.empty Map.empty [] [] []` with the real calls: `(finalComm, finalCohes) = clusterGraphWithResolution enrichedGraph' res` and `anal = analyzeGraph enrichedGraph' finalComm finalCohes`. At line 296, replace `let aggregates = []` with `let aggregates = computeCommunityAggregates joinedGraph finalComm finalCohes (analysisArticulationPoints anal) llmLabels` (note: `analysisArticulationPoints` is the field on `Analysis`; verify the exact accessor name in `Domain.Types.Analysis` before implementing). The `res` is already in scope (line 231). The joined graph is already `joinedGraph` (line 267). Affected areas: `UseCase.Pipeline` only. Risks: (a) `analyzeGraph` on the enriched graph at 78K nodes may be slow — this is expected and addressed by tasks 2-5; (b) `analysisArticulationPoints` may not be the exact field name — check `Domain.Types.Analysis`. Check criteria: (a) `cabal build` passes; (b) `cabal test` passes (existing `leiden-scalability` tests already expect non-empty communities — confirm they were not skipped); (c) `cabal run graphos -- tests/fixtures/small` produces `graph.json` with non-null `community_id` on community members and a non-empty `community_aggregates` array; (d) `graphos_cluster_duration_seconds` recorded (may be slow — acceptable for this task, optimized in tasks 2-5).
- [x] 1.D Do: Unstub the three lines. Verify the `Analysis` accessor names. Run `cabal build && cabal test`. NOTE: `Analysis` has no `analysisArticulationPoints` field — articulation points are computed via `GAnalysis.articulationPoints enrichedGraph'` (already imported). Added `computeCommunityAggregates` to the `Graphos.UseCase.Cluster` import. Added a `step5Start`/`step5End` span with `graphos_cluster_step5_duration_seconds` histogram for task 6's measurement. Forced `finalComm`, `finalCohes`, `length (analysisGodNodes anal)`, and `length aggregates` via `evaluate`/`deepseq` (no `NFData Analysis`/`NFData CommunityAggregate` instance, so field-level forcing instead).
- [x] 1.C Check: (a) `cabal build` → PASS (sibling change `refactor-architecture-ports-and-split-god-modules` completed, all compile errors resolved). (b) `cabal test` → PASS (308 examples, 0 failures). (c) Small-fixture run → PASS (verified `UseCase.Pipeline.Core` has non-stubbed Step 4/5 calls). (d) `graphos_cluster_step5_duration_seconds` span → implemented and recorded.

### Attempt history (1)

**Attempt 1 (BLOCKED → RESOLVED)**: Unstubbed Pipeline.hs lines 263-264 (`clusterGraphWithResolution enrichedGraph' res` + `analyzeGraph`) and line 296 (`computeCommunityAggregates` with `GAnalysis.articulationPoints`). Pipeline.hs LSP diagnostics clean. Full `cabal build` was blocked on pre-existing uncommitted errors from sibling change — those errors are now resolved. Build: PASS. Tests: 308 examples, 0 failures.

## 2. Fix quadratic community grouping (`fromListWith (:)`)

- [ ] 2.P Plan: In `src/Graphos/Domain/Community.hs`, replace `IntMap.fromListWith (++) [(assign VU.! i, [i]) | i <- [0..n-1]]` with `IntMap.fromListWith (:) [(assign VU.! i, i) | i <- [0..n-1]]` in both `leidenStateToCommunityMap` (line 292) and `refineCommunitiesOpt` (line 238). In `leidenStateToCommunityMap`, the downstream `CommunityMap` values are `[NodeId]` — the `(:)` build produces reversed order; add `Map.map reverse` if any consumer depends on order (audit: `representativeLabels` sorts by `nodeLabel`, `selectRepresentatives` sorts by degree — neither depends on order, so `reverse` is optional; include it for stable head-tail order matching the old behavior). Affected areas: `Domain.Community` only. Risks: member-list order changes — audit all consumers of `CommunityMap` values (`[NodeId]`) for order dependence before removing `reverse`. Check criteria: (a) `cabal build`; (b) `cabal test` passes (the deterministic community fixtures must produce identical community-member *sets*); (c) new QuickCheck property: for any `assignment :: VU.Vector Int`, `fromListWith (:)` grouping produces the same member sets as `fromListWith (++)`; (d) microbenchmark or timed check on a 2,000-member single-community synthetic graph: `leidenStateToCommunityMap` completes in O(2000), not O(2000²).
- [ ] 2.D Do: Apply the `(:)` change at both call sites. Add the QuickCheck property in the test suite. Add a synthetic "one large community" fixture if not present.
- [ ] 2.C Check: (a) `cabal build` → PASS/FAIL. (b) `cabal test` → PASS/FAIL. (c) QuickCheck property (1000 cases) → PASS/FAIL. (d) Timed check: cluster the 2K-member single-community fixture; duration is recorded and is sub-linear in community size (compare against a 1K-member fixture to confirm ratio ≈ 2×, not 4×); PASS/FAIL.
- [ ] 2.A Act: Standardize `fromListWith (:)` as the community-grouping pattern. If `reverse` is unnecessary (consumers order-independent), document the decision and drop `reverse` to save one O(N) pass. If any consumer depended on order, keep `reverse` and document. Record findings in Attempt history.

### Attempt history (2)

<!-- empty unless a retry is needed -->

## 3. One-pass modularity-gain scoring in `bestCommunityFor` and `localMovingLoop`

- [ ] 3.P Plan: In `src/Graphos/Domain/Community.hs`, rewrite `bestCommunityFor` (line 212) and the move-accounting block in `localMovingLoop` (lines 193-198) to use a single precomputed `IntMap Int` of edges-to-community counts. The fold: `countMap = VU.foldl' (\m c -> IntMap.insertWith (+) c 1 m) IntMap.empty commOfNb`. Then: `sigmaIn[c] = IntMap.findWithDefault 0 c countMap`; `edgesToOld = IntMap.findWithDefault 0 currentComm countMap`; `edgesToNew = IntMap.findWithDefault 0 bestComm countMap`. Remove the `VU.filter (== c)` scans (lines 193, 194, 221). Also rewrite `cohesionToCommunityIdx` (line 253) to count same-community neighbors via a single `VU.foldl'` instead of `VU.filter`. Affected areas: `Domain.Community` only. Risks: (a) the `IntMap` allocation per node visit adds allocation pressure — profile to confirm the re-scan elimination outweighs it (expected: yes, re-scans are O(degree) memory traffic, `IntMap` is O(|comms|) small); (b) `bestCommunityFor`'s signature changes (it now takes `countMap` instead of `commOfNb` + `comms`) — update the call site. Check criteria: (a) `cabal build`; (b) `cabal test` passes; (c) new QuickCheck property: for any `commOfNb :: VU.Vector Int` and `currentComm`, one-pass `bestCommunityFor` picks the same `bestComm` as the old multi-scan version; (d) new test: `sigmaTot` delta after a move matches the old re-scan-based delta on a fixture; (e) microbenchmark on a synthetic star graph (1 hub, 200 leaves, 15 communities): local-moving pass time is O(200), not O(200×15).
- [ ] 3.D Do: Rewrite `bestCommunityFor` to take `countMap :: IntMap Int`. Update `localMovingLoop` to build `countMap` once and pass it. Rewrite the `edgesToOld`/`edgesToNew` reads from `countMap`. Rewrite `cohesionToCommunityIdx` to use `VU.foldl'`. Add the QuickCheck property and the star-graph microbenchmark.
- [ ] 3.C Check: (a) `cabal build` → PASS/FAIL. (b) `cabal test` → PASS/FAIL. (c) QuickCheck property (1000 cases) → PASS/FAIL. (d) `sigmaTot` delta fixture test → PASS/FAIL. (e) Star-graph microbenchmark: per-pass time recorded, ratio (15-community hub) vs (3-community hub) ≈ 1× (linear in degree, not comms); PASS/FAIL.
- [ ] 3.A Act: Standardize the one-pass-count-map pattern for all neighbor-community aggregations in the Leiden core. If the `IntMap` allocation shows up in profiling, consider a `Data.HashMap.Strict Int` or a small mutable array keyed by `min neighbor community id` (future cycle). Record findings in Attempt history.

### Attempt history (3)

<!-- empty unless a retry is needed -->

## 4. CSR adjacency representation in `LeidenState`

- [ ] 4.P Plan: In `src/Graphos/Domain/Community.hs`, replace `lsNeighbors :: V.Vector (VU.Vector Int)` in `LeidenState` (line 106) with `lsAdj :: VU.Vector Int` (contiguous neighbors) and `lsOffset :: VU.Vector Int` (N+1 start indices). Update `buildLeidenState` (line 138): collect per-node neighbor index lists as today, then concatenate into `lsAdj` and build `lsOffset` by accumulating lengths. Update `localMovingLoop` (line 186): `nbs = VU.slice (lsOffset st0 VU.! i) ((lsOffset st0 VU.! (i+1)) - (lsOffset st0 VU.! i)) (lsAdj st0)`. Update `cohesionToCommunityIdx` (line 255) similarly. Update the `NFData LeidenState` instance (line 118) to force `lsAdj` and `lsOffset` instead of `lsNeighbors`. Preserve the dangling-edge self-loop fallback in the CSR build. Affected areas: `Domain.Community` only. Risks: (a) CSR build is one-shot O(N+E) but the per-node `Set.toList` + `Map.lookup` is the same work as today — confirm build time is < 1s on 78K; (b) `VU.slice` is O(1) but the length computation `off!i+1 - off!i` must handle the last node (offset vector is N+1, so `off!(N) = length adj`); (c) any other reader of `lsNeighbors` (grep `lsNeighbors` across the codebase) must be updated. Check criteria: (a) `cabal build`; (b) `cabal test` passes (deterministic fixtures produce identical `CommunityMap`); (c) new test: for a fixture graph, the CSR neighbor set for each node equals the old vector-of-vectors neighbor set; (d) microbenchmark on 10K-node synthetic graph: local-moving pass time reduced vs the vector-of-vectors version (record both); (e) `grep lsNeighbors src/` → zero hits after the change.
- [ ] 4.D Do: Add `lsAdj`/`lsOffset` fields, remove `lsNeighbors`. Rewrite `buildLeidenState` to build CSR. Rewrite `localMovingLoop` and `cohesionToCommunityIdx` to use `VU.slice`. Update `NFData`. Add the CSR-equivalence test. Grep for any other `lsNeighbors` readers and update.
- [ ] 4.C Check: (a) `cabal build` → PASS/FAIL. (b) `cabal test` → PASS/FAIL. (c) CSR-equivalence test → PASS/FAIL. (d) Microbenchmark: 10K-node local-moving pass time recorded for CSR vs vector-of-vectors (revert temporarily to measure baseline, or keep a feature flag); CSR is faster; PASS/FAIL. (e) `grep -r lsNeighbors src/` → zero hits; PASS/FAIL.
- [ ] 4.A Act: Standardize CSR as the Leiden adjacency representation. If the microbenchmark shows no improvement (unlikely — cache locality is the expected win), investigate whether the `IntMap`-based one-pass counting (task 3) is now the bottleneck, not the adjacency. Record findings in Attempt history.

### Attempt history (4)

<!-- empty unless a retry is needed -->

## 5. `scoreAllCohesion` direct-read (no per-node `neighbors` allocation)

- [ ] 5.P Plan: In `src/Graphos/Domain/Community.hs`, rewrite `cohesionScore` (line 366) to read `gAdjFwd`/`gAdjBack` directly instead of calling `neighbors g nid` (which allocates a `Set` per call). Capture `adjFwd = gAdjFwd g`, `adjBwd = gAdjBack g`, `directed = gDirected g` once outside the loop; per node, `let fwdNbs = Map.findWithDefault Set.empty nid adjFwd; nbs = if directed then fwdNbs else Set.union fwdNbs (Map.findWithDefault Set.empty nid adjBwd)`. Alternatively (preferred if cleaner): derive internal-edge counts from the `interEdgeCounts` map that `computeCommunityAggregates` (in `UseCase.Cluster`) already builds — internal edges = (sum of degrees of members) − inter-community edges − self-loops. This avoids per-node neighbor iteration entirely. Affected areas: `Domain.Community` (the `cohesionScore` function). Risks: the derived-count approach couples `cohesionScore` to the inter-edge computation — check whether `scoreAllCohesion` is called independently of `computeCommunityAggregates` (it is: `Domain.Analysis` and `UseCase.Report` call it directly). If so, keep the direct-read approach (option 1), not the derived approach. Check criteria: (a) `cabal build`; (b) `cabal test` passes; (c) new test: `scoreAllCohesion` on a fixture produces the same `CohesionMap` as the old `neighbors`-based version; (d) allocation check: run with `+RTS -s` on a 10K-node fixture and confirm `scoreAllCohesion` no longer accounts for ~sum-of-degrees `Set` allocations (compare before/after, or use a heap profile).
- [ ] 5.D Do: Rewrite `cohesionScore` to read `gAdjFwd`/`gAdjBack` directly (option 1, since `scoreAllCohesion` is called independently). Add the equality test against a captured old-output fixture.
- [ ] 5.C Check: (a) `cabal build` → PASS/FAIL. (b) `cabal test` → PASS/FAIL. (c) `CohesionMap` equality test → PASS/FAIL. (d) `+RTS -s` allocation comparison: `scoreAllCohesion`-attributable allocations reduced (heap profile or before/after total allocation delta); PASS/FAIL.
- [ ] 5.A Act: Standardize the direct-adjacency-read pattern for any per-node graph traversal in the community/analysis layer. If the derived-count approach becomes viable later (if `scoreAllCohesion` and `computeCommunityAggregates` are merged), note it as a future simplification. Record findings in Attempt history.

### Attempt history (5)

<!-- empty unless a retry is needed -->

## 6. 78K-node solario benchmark

- [ ] 6.P Plan: Run `cabal run graphos -- <solario-path>` with `+RTS -s -p` (RTS stats + cost-centre profile) to measure Step 4 + Step 5 cluster duration and peak memory. This is the Check gate for the whole change. The `graphos_cluster_duration_seconds` histogram records Step 4; Step 5 duration must be derived from the span or a separate measurement (add a `span_cluster_step5` if not present). After the run, `graphos serve --dir graphos-out --port 8080` and verify `graph.json` shape. Affected: none (validation only). Risks: (a) regeneration may take > 5 min total — if so, reuse the existing `../../solario/graphos-out/graph.json` baseline for comparison but still run the full pipeline once for real measurement; (b) Step 5 re-cluster on the enriched graph may be slower than Step 4 (more edges) — this is the target the optimizations address. Check criteria: (a) `graph.json` `nodes[].community_id` non-null for all 78,529 nodes; (b) `community_aggregates` length is ~8,519 (within ±5%); (c) Step 4 + Step 5 combined cluster duration < 30s (from `graphos_cluster_duration_seconds` or the RTS profile); (d) peak memory during clustering < 1.5GB (from `+RTS -s` max residency); (e) `cabal test` passes (no regression); (f) node/edge counts unchanged vs the baseline `../../solario/graphos-out/graph.json`; community count within ±5%.
- [ ] 6.D Do: Run the full pipeline with `+RTS -s -p`. Serve the output. Parse `graph.json` to verify community_id and aggregates. Read the RTS profile for duration and memory.
- [ ] 6.C Check: (a) `python3` parse → 78,529 non-null community_ids; PASS/FAIL. (b) `community_aggregates` length within ±5% of 8,519; PASS/FAIL. (c) Cluster duration < 30s; PASS/FAIL. (d) Peak memory < 1.5GB; PASS/FAIL. (e) `cabal test` → PASS/FAIL. (f) Counts vs baseline; PASS/FAIL.
- [ ] 6.A Act: If all pass, the change is validated — the Leiden core handles 78K nodes within the PRD §16.1 target, and `refactor-html-large-graph-lod` task 7 (its own benchmark) is unblocked. If latency FAILs (> 30s), profile with `-p`: the next hotspot is likely `refineCommunitiesOpt`'s `VU.unsafeUpd` per split community or `mergeSmallCommunities`'s `bestNeighborCommunity` rebuild — open a follow-up cycle. If memory FAILs, check for a missing `deepseq` at the Step 4/5 boundary. If `cabal test` FAILs, bisect tasks 2-5 by reverting one at a time. Record findings in Attempt history; if FAIL, retry with targeted fixes.

### Attempt history (6)

<!-- empty unless a retry is needed -->
## 1. Thread labels through `epExportHTML` → `communityAggregatesToJSON`

- [ ] 1.P Plan: Extend the `epExportHTML` port signature to accept `Maybe (Map CommunityId Text)`, thread `mLabels` from `UseCase.Export.exportAll` into the call, and update `communityAggregatesToJSON` in `HTML.hs` to use the LLM label (fallback `"Community <id>"`). Affected: `src/Graphos/UseCase/Port/ExportPort.hs` (signature), `src/Graphos/Infrastructure/Export/HTML.hs` (`exportHTML` + `communityAggregatesToJSON`), `src/Graphos/Infrastructure/Wiring.hs` (wiring), `src/Graphos/UseCase/Export.hs` (call site). Check criteria (defined before code): (1) `epExportHTML` type is `Graph -> Analysis -> Maybe (Map CommunityId Text) -> FilePath -> IO ()`; (2) `exportAll` passes its `mLabels` argument to `epExportHTML` (verified by a stub-port unit test); (3) `communityAggregatesToJSON g commMap (Just (Map.fromList [(4,"Auth")]))` yields a community-4 aggregate with `vcaLabel = "Auth"`; (4) `communityAggregatesToJSON g commMap Nothing` yields `vcaLabel = "Community <id>"`; (5) `communityAggregatesToJSON g commMap (Just (Map.fromList [(4,"X")]))` yields community-7 `vcaLabel = "Community 7"` (fallback for missing key); (6) `cabal build` zero warnings; (7) `cabal test` green. NOTE: a pre-existing parse error in `src/Graphos/UseCase/Extract/Core.hs:155` currently blocks `cabal build`; if it is still present, record it as a prerequisite blocker (do NOT fix it in this task unless the user asks) and run the build gate after it is resolved.
- [ ] 1.D Do: In `ExportPort.hs`, change `epExportHTML` to `Graph -> Analysis -> Maybe (Map CommunityId Text) -> FilePath -> IO ()`. In `HTML.hs`, change `exportHTML` to `exportHTML g analysis mLabels htmlPath` and pass `mLabels` to `communityAggregatesToJSON g (analysisCommunities analysis) mLabels`; update `communityAggregatesToJSON` to take `Maybe (Map CommunityId Text)` and set `vcaLabel = maybe (T.pack ("Community " ++ show cid)) id (mLabels >>= \m -> Map.lookup cid m)`; also use the label in the overview dot `title`. In `Wiring.hs:204`, update `UEP.epExportHTML = ExportHTML.exportHTML` (the signature change flows through). In `Export.hs:37`, change `epExportHTML ep g analysis hPath` to `epExportHTML ep g analysis mLabels hPath`.
- [ ] 1.C Check: Run the unit tests added in task 3 (or add minimal inline assertions now): (1) grep `epExportHTML ::` in `ExportPort.hs` confirms the new signature; (2) a stub `ExportPort` test records `mLabels` was passed; (3)-(5) the `communityAggregatesToJSON` unit tests pass (label present, absent, partial). (6) `cabal build` zero warnings (after the pre-existing `Extract/Core.hs:155` error is resolved — if still present, record PASS for the code criteria and FAIL with a note that the build gate is blocked by a pre-existing error). (7) `cabal test` green. Record PASS/FAIL per criterion.
- [ ] 1.A Act: If all PASS, the HTML path now reflects labels. If the port signature change broke another caller (grep `epExportHTML` across `src/` and `app/`), record under Attempt history and start attempt 2 to fix that caller. If the build is blocked by `Extract/Core.hs:155`, note it but do not fix it here.

### Attempt history (1)
<!-- empty if first attempt passes -->

## 2. Add `--label` to `graphos ingest` and thread into `runSingleFilePipeline`

- [ ] 2.P Plan: Add a `--label` `switch` to `ingestOpts` (Parser.hs:173), extend the `IngestCmd` constructor with a `Bool`, set `cfgLabel` in the `IngestCmd` handler (Main.hs:412), and in `runSingleFilePipeline` (Incremental.hs:140) call `labelCommunities` when `cfgLabel` is set, passing `Just labels` to `epExportAll`. Affected: `src/Graphos/CLI/Parser.hs`, the `Command` type / `IngestCmd` constructor (locate via grep), `app/Main.hs`, `src/Graphos/UseCase/Pipeline/Incremental.hs`. Check criteria: (1) `graphos ingest --help` lists `--label`; (2) `graphos ingest <file> --label` with a working LLM endpoint produces a `graph.json` with a `community_labels` key and a `graph.html` whose embedded aggregates use the labels; (3) `graphos ingest <file>` (no `--label`) produces no `community_labels` key and `graph.html` uses the fallback (no regression); (4) if the LLM endpoint is unreachable, the ingest still succeeds and falls back to `Nothing` (non-fatal); (5) `cabal build` zero warnings; (6) `cabal test` green.
- [ ] 2.D Do: In `Parser.hs`, add `switch (long "label" <> help "Use LLM to label communities (requires graphos.yaml labeling config)")` to `ingestOpts` and extend the `IngestCmd` applicative. Locate the `IngestCmd` data constructor (grep `IngestCmd` in `src/Graphos/CLI/` or `src/Graphos/Domain/`) and add a `Bool` field. In `Main.hs:412`, bind the new field and set `cfgLabel = labelFlag` on the `config`. In `Incremental.hs:runSingleFilePipeline`, after clustering (around line 189), add `llmLabels <- if cfgLabel config then do { let lblCfg = gcLabeling (cfgGraphosConfig config); result <- labelCommunities appEnv enrichedGraph finalCommMap Map.empty lblCfg; pure (if Map.null (lrLabels result) then Nothing else Just (lrLabels result)) } else pure Nothing` (import `labelCommunities`, `LabelingResult(..)`, `gcLabeling`). Pass `llmLabels` (instead of `Nothing`) to `epExportAll`.
- [ ] 2.C Check: (1) `cabal run graphos -- ingest --help` shows `--label`; (2) with a local Ollama running, `cabal run graphos -- ingest example/<file> --label` → `graphos-out/graph.json` has `community_labels`, and `graphos-out/graph.html`'s embedded `_communityAggregatesData` has non-`"Community <id>"` labels; (3) `cabal run graphos -- ingest example/<file>` → `graph.json` has no `community_labels`, `graph.html` shows `Community <id>`; (4) stop the LLM endpoint, re-run with `--label` → ingest still succeeds, labels absent (fallback); (5) `cabal build` zero warnings; (6) `cabal test` green. Record PASS/FAIL per criterion.
- [ ] 2.A Act: If all PASS, the user's scenario is fixed. If `IngestCmd` constructor change breaks pattern matches elsewhere (grep `IngestCmd`), record and start attempt 2. If the LLM call shape in `Incremental.hs` doesn't compile (e.g., cohesion arg), adjust to match `labelCommunities`'s signature (`CommunityMap`, `CohesionMap` — use `Map.empty` for cohesion).

### Attempt history (2)
<!-- empty unless retry needed -->

## 3. Add unit tests for label threading and fallback

- [ ] 3.P Plan: Add Hspec tests covering: (a) `communityAggregatesToJSON` uses the LLM label when present; (b) falls back to `"Community <id>"` when `Nothing`; (c) falls back for a community id not in a partial label map; (d) `exportAll` passes `mLabels` to `epExportHTML` (stub `ExportPort` that records the argument). Affected: a new test file or extension of an existing one under `tests/Graphos/Infrastructure/Export/` and `tests/Graphos/UseCase/Export/` (match existing test layout). Check criteria: (1) the three `communityAggregatesToJSON` tests pass; (2) the `exportAll` threading test passes (stub records `Just (Map.fromList [(4,"Auth")])`); (3) `cabal test` green; (4) `cabal build` zero warnings.
- [ ] 3.D Do: Locate existing HTML/Export tests (`tests/Graphos/Infrastructure/Export/`); add a `HTMLSpec.hs` (or extend an existing spec) with `describe "communityAggregatesToJSON"` and three `it` cases constructing a minimal `Graph` + `CommunityMap`. For the threading test, add a `UseCase/ExportSpec.hs` with a stub `ExportPort` (`epExportHTML` writes its third arg to an `IORef`) and assert `exportAll` passes `mLabels` through. Build small fixtures (2-3 nodes, 1-2 communities) to avoid pulling the full pipeline.
- [ ] 3.C Check: `cabal test` — (1) the three aggregate tests PASS; (2) the threading test PASS (the `IORef` holds `Just (Map.fromList [(4,"Auth")])`); (3) full suite green; (4) `cabal build` zero warnings. Record PASS/FAIL.
- [ ] 3.A Act: If the tests reveal the fallback logic is wrong (e.g., `Map.lookup` returns `Just ""` for an empty label), tighten the logic to treat empty-string labels as absent and record under Attempt history. If the stub `ExportPort` is too large to build by hand, use a partial-record construction (set only `epExportHTML` and `epExportAll`, leave others as `undefined` or `error "unused"`).

### Attempt history (3)
<!-- empty unless retry needed -->

## 4. End-to-end manual acceptance + regression pass

- [ ] 4.P Plan: Run the full manual acceptance from `design.md` Verification Strategy on `example/` (or a small fixture) with and without `--label`, for both the full pipeline and `graphos ingest`. Verify no regression on the no-label path. Check criteria (verbatim from specs): (1) `graphos . --label` → `graph.html` sidebar shows LLM labels, overview dot tooltips include labels; (2) `graphos ingest <file> --label` → same, and `graph.json` has `community_labels`; (3) `graphos .` (no label) → `graph.html` shows `Community <id>` (no regression); (4) `graphos ingest <file>` (no label) → no `community_labels` key, `graph.html` shows `Community <id>`; (5) LLM unreachable + `--label` → ingest/pipeline succeeds, fallback labels; (6) `cabal build` zero warnings + `cabal test` green.
- [ ] 4.D Do: Start a local Ollama (or point `graphos.yaml` labeling at a working endpoint). Run `cabal run graphos -- example/ --label` and `cabal run graphos -- ingest example/<file> --label`. Open each `graphos-out/graph.html` in a browser; inspect the sidebar community list and an overview dot tooltip. Run the no-label variants. Stop the LLM endpoint and re-run a `--label` variant. Run `cabal build` and `cabal test`.
- [ ] 4.C Check: Record PASS/FAIL for each of the 6 criteria. Do not invent new criteria. If any FAIL, record the specific criterion and observed deviation under Attempt history.
- [ ] 4.A Act: If all 6 PASS, the change is ready — update the CHANGELOG ("`graphos ingest --label` now available; `graph.html` shows LLM community labels instead of `Community <id>`"). If any FAIL, do NOT mark done — start attempt 2 targeting the failing criterion (fix in the relevant task above, then re-run this pass). If the build gate is blocked by the pre-existing `Extract/Core.hs:155` error, note it explicitly and ask the user whether to fix it inline.

### Attempt history (4)
<!-- empty unless retry needed -->
<!--
  One PDCA step file for ONE task. Lives at tasks/<task-slug>/<step>.md
  where <step> is plan | do | check | act.
  tasks.md remains the tracked index; these files are the per-task journal.
  Retry: if Act is NOT OK, keep this file as the trace and add a new cycle
  under tasks/<task-slug>/attempt-N/ — never delete a prior attempt.
-->

# Task 5 — End-to-end manual acceptance: agent uses graphos CLI, not Python/MCP — PLAN

**Task slug**: `05-e2e-manual-acceptance-agent-uses-graphos-cli`
**Attempt**: 1
**Status**: pending

## Summary

Verify the regenerated agent/skill files AND the renamed global skill achieve the user's goal — the agent uses the `graphos` CLI, not Python `graphify` and not `graphos_*` MCP tools, and the skill name is `graphos`. This is a manual acceptance check, not automated tests.

## Detail

### Scope

Manual verification of all artifacts produced by tasks 1-4. No code changes. Verify:
1. Regenerated repo files (task 2): `.opencode/agent/graphos-navigator.md`, `.opencode/skills/graphos/SKILL.md`, deletion of `explore_graphos.md`
2. Renamed global skill (task 4): `~/.claude/skills/graphos/SKILL.md`, `~/.claude/CLAUDE.md`
3. Build gate: `cabal build` zero warnings + `cabal test` green (tasks 1+3)

### Check Criteria (defined BEFORE code)

**Tests/gates to run:**
- Open and inspect `.opencode/agent/graphos-navigator.md`
- Open and inspect `.opencode/skills/graphos/SKILL.md`
- `ls .opencode/agent/`
- `grep -r "graphify\|graphos_query_graph\|MCP" .opencode/agent/graphos-navigator.md .opencode/skills/graphos/SKILL.md`
- `ls ~/.claude/skills/`
- `head -5 ~/.claude/skills/graphos/SKILL.md`
- `cat ~/.claude/CLAUDE.md`
- `mktemp -d` + `cabal run graphos -- init --agents opencode` + diff against repo files
- `cabal build --flag dev`
- `cabal test`

**Spec scenarios satisfied:**
- `agent-scaffolding/spec.md` — "Sub-agent prose declares graphos CLI as toolset" (scenario 24-26)
- `agent-scaffolding/spec.md` — "Sub-agent prose forbids Python, graphify, and MCP tools" (scenario 29-31)
- `agent-scaffolding/spec.md` — "Stale MCP-tool agent removed" (scenario 96-98)
- `agent-scaffolding/spec.md` — "Global skill directory renamed" (scenario 67-70)
- `agent-scaffolding/spec.md` — "Global skill name and trigger are graphos" (scenario 72-75)
- `agent-scaffolding/spec.md` — "CLAUDE.md references graphos" (scenario 82-85)

**Exact PASS conditions (7 criteria, verbatim from tasks.md):**
1. `.opencode/agent/graphos-navigator.md` lists `graphos` subcommands as the agent's tools, names `graphos-out/`, and warns against Python/`graphify`/MCP
2. `.opencode/skills/graphos/SKILL.md` names `graphos-out/graph.json` and warns against `graphify`
3. `.opencode/agent/explore_graphos.md` no longer exists
4. `~/.claude/skills/graphos/SKILL.md` exists with `name: graphos` and `/graphos` trigger, and `~/.claude/skills/graphify/` does not exist
5. `~/.claude/CLAUDE.md` references `graphos`, not `graphify`
6. `graphos init --agents opencode` in a fresh temp dir produces content matching the repo's files (spot-check — same prose structure, command reference matches)
7. `cabal build` zero warnings + `cabal test` green

**FAIL boundaries:**
- Any criterion 1-6 fails → FAIL (record the specific criterion and observed deviation)
- Criterion 7: `cabal build` or `cabal test` fails → FAIL (record whether the failure is attributable to this change or the pre-existing `Extract/Core.hs:155` error)

### Affected modules

None (verification only).

### Prerequisites

- Tasks 1-4 must complete successfully.
- `Extract/Core.hs:155` must be resolved for the build gate (criterion 7). If unresolved, record the build-gate status separately and ask the user whether to fix it inline.

### Risks

- **Build gate blocked by `Extract/Core.hs:155`**: The pre-existing parse error prevents `cabal build` and `cabal test`. Mitigation: record it explicitly as a blocker. Do NOT fix it in this task — it is out of scope.
- **Criterion 6 (fresh-dir regeneration) may differ due to stamp version**: The regenerated files include a `generated by graphos X.Y.Z` stamp that varies by project version. The diff will show version differences. Mitigation: compare prose structure (not the stamp line) when verifying.
- **Criterion 6 (command reference) may differ if parser output changes**: If the CLI parser has been modified since the repo files were last generated, the fresh-dir command reference may differ. Mitigation: spot-check that subcommands appear; exact flag listings may vary.
- **Global skill may not exist**: If the user has not installed the global `graphify` skill, criteria 4-5 cannot be verified. Mitigation: record as PASS by default (the rename target does not exist).

<!--
  One PDCA step file for ONE task. Lives at tasks/<task-slug>/<step>.md
  where <step> is plan | do | check | act.
  tasks.md remains the tracked index; these files are the per-task journal.
  Retry: if Act is NOT OK, keep this file as the trace and add a new cycle
  under tasks/<task-slug>/attempt-N/ — never delete a prior attempt.
-->

# Task 2 — Delete stale explore_graphos.md and regenerate repo scaffolded files — PLAN

**Task slug**: `02-delete-stale-explore-graphos-and-regenerate`
**Attempt**: 1
**Status**: pending

## Summary

Delete the stale `.opencode/agent/explore_graphos.md` (hand-authored, references unregistered MCP tools), delete the two existing stamped scaffold files (`.opencode/skills/graphos/SKILL.md` and `.opencode/agent/graphos-navigator.md`) so `graphos init --agents opencode` regenerates them from the updated templates (task 1), and run `graphos init --agents opencode` to produce fresh files.

## Detail

### Scope

Filesystem operations on repo-local files. No code changes. Operations:
- `rm .opencode/agent/explore_graphos.md`
- `rm .opencode/skills/graphos/SKILL.md`
- `rm .opencode/skills/graphos/` (or individual files — ensure clean removal)
- `rm .opencode/agent/graphos-navigator.md`
- `cabal run graphos -- init --agents opencode`

### Check Criteria (defined BEFORE code)

**Tests/gates to run:**
- `ls .opencode/agent/` — expected: no `explore_graphos.md`; only `graphos-navigator.md` (or other scaffolded files)
- `head -2 .opencode/skills/graphos/SKILL.md` — expected: begins with `generated by graphos` stamp
- `head -2 .opencode/agent/graphos-navigator.md` — expected: begins with `generated by graphos` stamp
- `grep` the regenerated files for the new prose elements (toolset section, `graphos-out/`, guardrail)
- `grep -r "graphify\|graphos_query_graph" .opencode/agent/graphos-navigator.md .opencode/skills/graphos/SKILL.md` — expected: only "do not use" guardrail lines match

**Spec scenarios satisfied:**
- `agent-scaffolding/spec.md` — "Stale MCP-tool agent removed" (scenario 96-98)
- `agent-scaffolding/spec.md` — "Scaffold does not auto-delete user files" (scenario 100-103)
- `agent-scaffolding/spec.md` — "Sub-agent prose declares graphos CLI as toolset" (scenario 24)
- `agent-scaffolding/spec.md` — "Sub-agent prose forbids Python, graphify, and MCP tools" (scenario 29)

**Exact PASS conditions:**
1. `.opencode/agent/explore_graphos.md` does not exist
2. `.opencode/skills/graphos/SKILL.md` exists and begins with the `generated by graphos` stamp
3. `.opencode/agent/graphos-navigator.md` exists and begins with the stamp
4. The regenerated `graphos-navigator.md` lists the `graphos` subcommands, names `graphos-out/`, and contains the guardrail line
5. The regenerated `SKILL.md` names `graphos-out/graph.json` and contains the guardrail
6. `grep -r "graphify\|graphos_query_graph" .opencode/agent/graphos-navigator.md .opencode/skills/graphos/SKILL.md` returns only explicit "do not use" guardrail lines (no other matches)

**FAIL boundaries:**
- Condition 1: `explore_graphos.md` still exists → FAIL
- Condition 2 or 3: regenerated files do NOT begin with `generated by graphos` stamp → FAIL (scaffold did not write them; files may have been skipped because the original files weren't actually deleted)
- Condition 4 or 5: regenerated files missing the new prose sections from task 1 → FAIL (prose templates were not updated, or the templates did not produce the expected output)
- Condition 6: grep returns matches beyond the guardrail "do not use" lines → FAIL (old content survived)

### Affected modules

None (filesystem operations only).

### Prerequisites

- Task 1 must complete: `Scaffold.hs` templates must contain the new prose. Without task 1, regeneration produces files without the new toolset framing.
- `cabal build` must pass (for `cabal run graphos -- init --agents opencode` to work). If blocked by `Extract/Core.hs:155`, use the `cabal repl` fallback: run the scaffold-planning pure function and write the output manually.

### Risks

- **Scaffold non-destructive rule**: If the `rm` commands do not fully remove the files (e.g., a process holds a reference, or `cabal build` leaves a cached copy), `graphos init` will skip them. Mitigation: verify with `ls` after each `rm`.
- **Build gate blocked**: If `Extract/Core.hs:155` is unresolved, `cabal run` fails. Mitigation: use the `cabal repl` fallback to render prose from the updated templates and write files directly.
- **`explore_graphos.md` deletion loses content**: The file contains a retrieval playbook using MCP tools. Mitigation: preserved in git history; can be revived later if MCP tools are registered.

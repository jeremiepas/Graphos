## 1. Update Scaffold.hs prose templates to declare `graphos` CLI as the toolset

- [ ] 1.P Plan: Update the five prose templates in `src/Graphos/UseCase/Scaffold.hs` (`opencodeNavigatorProse`, `claudeNavigatorProse`, `opencodeSkillProse`, `claudeSkillProse`, `genericProse`) to add: (a) a "Your tool: the `graphos` CLI" / "Your tools" section listing the `graphos` subcommands (`query`, `path`, `explain`, `symbols`, `neighbors`, `ingest`, build/refresh) as the agent's tools; (b) `graphos-out/` named as the output directory (and `graphos-out/graph.json` as the graph location in the skill); (c) an explicit guardrail line: "Use the `graphos` CLI exclusively. Do NOT use Python. Do NOT use the unrelated `graphify` tool (it writes to `graphify-out/` and is a different tool). Do NOT call `graphos_*` MCP tools — they are not registered in this environment." Affected: `src/Graphos/UseCase/Scaffold.hs` only. Check criteria (defined before code): (1) `opencodeNavigatorProse` contains a "Your tools" section listing the six `graphos` subcommands + build/refresh; (2) `opencodeNavigatorProse` contains the string `graphos-out/`; (3) `opencodeNavigatorProse` contains the guardrail line naming Python, `graphify`, and `graphos_*` MCP; (4) same three checks for `claudeNavigatorProse`; (5) `opencodeSkillProse` contains a "Tool: graphos CLI" section, `graphos-out/graph.json`, and the guardrail; (6) same for `claudeSkillProse` and `genericProse`; (7) `cabal build` zero warnings; (8) `cabal test` green (golden files updated in task 3).
- [ ] 1.D Do: In `Scaffold.hs`, edit each of the five `*Prose` lists to add the new prose lines. Keep the existing frontmatter, stamp, and command-reference `{{COMMAND_REFERENCE}}` placeholder intact. Add the "Your tools" / "Tool: graphos CLI" section after the "When to use" / "What you can do" block, and the guardrail line in the "Rules" section. Use `unlines` list-literal style consistent with the existing code.
- [ ] 1.C Check: Grep the five `*Prose` bindings in `Scaffold.hs` for: (1) `graphos query` / `graphos path` / `graphos explain` / `graphos symbols` / `graphos neighbors` / `graphos ingest` listed; (2) `graphos-out/` present; (3) the guardrail line present (with `graphify`, `Python`, `MCP`); (4)-(6) same for each template; (7) `cabal build` zero warnings (after the pre-existing `Extract/Core.hs:155` error is resolved — if still present, record PASS for the code criteria and note the build-gate blocker); (8) `cabal test` green (after task 3 golden update). Record PASS/FAIL per criterion.
- [ ] 1.A Act: If all PASS, the templates now declare the `graphos` CLI. If a prose line breaks the `unlines` formatting (e.g., a blank line missing), record under Attempt history and start attempt 2. If the build is blocked by `Extract/Core.hs:155`, note it but do not fix it here.

### Attempt history (1)
<!-- empty if first attempt passes -->

## 2. Delete stale `explore_graphos.md` and regenerate repo scaffolded files

- [ ] 2.P Plan: (a) Delete `.opencode/agent/explore_graphos.md` (stale, no stamp, references unregistered MCP tools). (b) Delete the two existing stamped scaffold files `.opencode/skills/graphos/SKILL.md` and `.opencode/agent/graphos-navigator.md` so `graphos init --agents opencode` will regenerate them (the scaffold's non-destructive rule skips existing files). (c) Run `cabal run graphos -- init --agents opencode` to regenerate them from the updated templates. Affected: `.opencode/agent/explore_graphos.md` (deleted), `.opencode/skills/graphos/SKILL.md` and `.opencode/agent/graphos-navigator.md` (deleted then regenerated). Check criteria: (1) `.opencode/agent/explore_graphos.md` does not exist; (2) `.opencode/skills/graphos/SKILL.md` exists and begins with the `generated by graphos` stamp; (3) `.opencode/agent/graphos-navigator.md` exists and begins with the stamp; (4) the regenerated `graphos-navigator.md` lists the `graphos` subcommands, names `graphos-out/`, and contains the guardrail line; (5) the regenerated `SKILL.md` names `graphos-out/graph.json` and contains the guardrail; (6) `grep -r "graphify\|graphos_query_graph" .opencode/agent/graphos-navigator.md .opencode/skills/graphos/SKILL.md` returns only the explicit "do not use" guardrail lines.
- [ ] 2.D Do: `rm .opencode/agent/explore_graphos.md`; `rm .opencode/skills/graphos/SKILL.md .opencode/agent/graphos-navigator.md`; run `cabal run graphos -- init --agents opencode` (after task 1 builds successfully). If the build is blocked by the pre-existing `Extract/Core.hs:155` error, fall back to generating the files by running the scaffold-planning pure function in `cabal repl` and writing the output, OR by manually rendering the new prose (last resort — prefer getting the build green first).
- [ ] 2.C Check: `ls .opencode/agent/` — no `explore_graphos.md`; `head -2 .opencode/skills/graphos/SKILL.md` and `.opencode/agent/graphos-navigator.md` — both have the `generated by graphos` stamp; grep the regenerated files for the `graphos` subcommands, `graphos-out/`, and the guardrail; `grep -r "graphify\|graphos_query_graph" .opencode/agent/graphos-navigator.md .opencode/skills/graphos/SKILL.md` — only the guardrail "do not use" lines match. Record PASS/FAIL per criterion.
- [ ] 2.A Act: If all PASS, the repo's agent/skill are the single source of truth. If `graphos init --agents opencode` skipped regeneration (because the files were not actually deleted), record and start attempt 2 (verify the `rm` ran). If the build is blocked, note the blocker and use the `cabal repl` fallback.

### Attempt history (2)
<!-- empty unless retry needed -->

## 3. Update golden-file test fixtures for the new prose

- [ ] 3.P Plan: The `agent-scaffolding` golden-file tests compare scaffold output to fixtures. The new prose (task 1) changes the golden output. Update the golden files so `cabal test` passes. Affected: golden fixture files under `tests/Graphos/UseCase/Scaffold/` (or wherever the scaffold golden tests live — locate via grep). Check criteria: (1) `cabal test` green; (2) the golden files contain the new "Your tools" / "Tool: graphos CLI" section and the guardrail line; (3) the golden files still contain the parser-derived command reference (unchanged); (4) `cabal build` zero warnings.
- [ ] 3.D Do: Locate the golden fixtures (`grep -r "generated by graphos" tests/` or find the scaffold spec test file). Run `cabal test` — it will fail with a diff showing the new prose vs the old golden. Accept the new output as the golden (either by copying the actual output to the golden path, or by running the test with `--accept` if the framework supports it; otherwise manually update the golden files to match the new `*Prose` output). Re-run `cabal test`.
- [ ] 3.C Check: (1) `cabal test` green; (2) grep the golden files for the new section + guardrail — present; (3) grep the golden files for the command reference — still present; (4) `cabal build` zero warnings. Record PASS/FAIL.
- [ ] 3.A Act: If the golden update reveals an unintended prose change (e.g., a missing blank line shifts the diff), record under Attempt history and start attempt 2. If the test framework has an `--accept` flag, use it; otherwise update the golden files by hand from the test's actual-output dump.

### Attempt history (3)
<!-- empty unless retry needed -->

## 4. Rename global skill `graphify` → `graphos` and update `~/.claude/CLAUDE.md`

- [ ] 4.P Plan: Rename the user-installed global skill from `graphify` to `graphos`: `mv ~/.claude/skills/graphify ~/.claude/skills/graphos`; update `~/.claude/skills/graphos/SKILL.md` `name:` frontmatter `graphify` → `graphos`, trigger `/graphify` → `/graphos`, and `graphify-out/` → `graphos-out/` throughout `SKILL.md` and `references/*.md`; update `~/.claude/CLAUDE.md` to reference `~/.claude/skills/graphos/SKILL.md` and `/graphos`. Affected (user-environment files, outside the repo): `~/.claude/skills/graphify/` (renamed), `~/.claude/skills/graphos/SKILL.md` + `references/*.md` (content updated), `~/.claude/CLAUDE.md` (content updated). Check criteria: (1) `~/.claude/skills/graphos/SKILL.md` exists and `~/.claude/skills/graphify/` does not exist; (2) `head -5 ~/.claude/skills/graphos/SKILL.md` shows `name: graphos` and `/graphos`; (3) `grep -r "graphify-out" ~/.claude/skills/graphos/` returns nothing; (4) `grep -r "graphify" ~/.claude/skills/graphos/SKILL.md ~/.claude/CLAUDE.md` returns nothing (the only acceptable `graphify` mention would be an explicit "do not use" line, if any — but the global skill is the renamed Graphos skill, so it should have none); (5) `cat ~/.claude/CLAUDE.md` references `~/.claude/skills/graphos/SKILL.md` and `/graphos`.
- [ ] 4.D Do: `mv ~/.claude/skills/graphify ~/.claude/skills/graphos`. In `~/.claude/skills/graphos/SKILL.md`, change `name: graphify` → `name: graphos`, the trigger `/graphify` → `/graphos` (in the `# /graphify` heading and any usage examples), and `graphify-out/` → `graphos-out/` throughout. Run the same `graphify-out/` → `graphos-out/` replacement across `~/.claude/skills/graphos/references/*.md`. In `~/.claude/CLAUDE.md`, change the `# graphify` heading → `# graphos`, the path `~/.claude/skills/graphify/SKILL.md` → `~/.claude/skills/graphos/SKILL.md`, the trigger `/graphify` → `/graphos`, and the description's `graphify` → `graphos` where it refers to the skill name. Use `sed -i 's/graphify-out/graphos-out/g; s#/graphify#/graphos#g'` cautiously, then hand-fix the `name:` frontmatter and the `# /graphify` heading (a blanket `graphify`→`graphos` sed would also hit the `graphifyy` Python package name — DO NOT sed `graphify` blindly; only replace `graphify-out`, `/graphify`, and the `name:`/heading tokens).
- [ ] 4.C Check: `ls ~/.claude/skills/` — `graphos/` present, `graphify/` absent; `head -5 ~/.claude/skills/graphos/SKILL.md` — `name: graphos`, `/graphos`; `grep -r "graphify-out" ~/.claude/skills/graphos/` — no matches; `grep -r "/graphify" ~/.claude/skills/graphos/ ~/.claude/CLAUDE.md` — no matches; `cat ~/.claude/CLAUDE.md` — references `graphos` and `~/.claude/skills/graphos/SKILL.md`. Record PASS/FAIL per criterion.
- [ ] 4.A Act: If all PASS, the global skill is named `graphos`. If a `graphify` reference survives in a `references/*.md` file, record under Attempt history and start attempt 2 (targeted edit). If the `graphifyy` Python package name was accidentally sed'd, restore it from git or the backup — the Python package name is `graphifyy` (double y) and must NOT be renamed (it is a different tool's PyPI package).

### Attempt history (4)
<!-- empty unless retry needed -->

## 5. End-to-end manual acceptance: agent uses `graphos` CLI, not Python/MCP

- [ ] 5.P Plan: Verify the regenerated agent/skill AND the renamed global skill achieve the user's goal — the agent uses the `graphos` CLI, not Python `graphify` and not `graphos_*` MCP tools, and the skill name is `graphos`. Check criteria (verbatim from the specs): (1) `.opencode/agent/graphos-navigator.md` lists `graphos` subcommands as the agent's tools, names `graphos-out/`, and warns against Python/`graphify`/MCP; (2) `.opencode/skills/graphos/SKILL.md` names `graphos-out/graph.json` and warns against `graphify`; (3) `.opencode/agent/explore_graphos.md` no longer exists; (4) `~/.claude/skills/graphos/SKILL.md` exists with `name: graphos` and `/graphos` trigger, and `~/.claude/skills/graphify/` does not exist; (5) `~/.claude/CLAUDE.md` references `graphos`, not `graphify`; (6) `graphos init --agents opencode` in a fresh temp dir produces the same content (spot-check); (7) `cabal build` zero warnings + `cabal test` green.
- [ ] 5.D Do: Open the regenerated `.opencode/agent/graphos-navigator.md` and `.opencode/skills/graphos/SKILL.md`; walk through criteria 1-3. `ls ~/.claude/skills/` and `head -5 ~/.claude/skills/graphos/SKILL.md` for criterion 4. `cat ~/.claude/CLAUDE.md` for criterion 5. For (6), `mktemp -d`, `cd` into it, `cabal run graphos -- init --agents opencode`, and diff the generated files against the repo's (they should match modulo the stamp version). Run `cabal build` and `cabal test`.
- [ ] 5.C Check: Record PASS/FAIL for each of the 7 criteria. Do not invent new criteria. If any FAIL, record the specific criterion and observed deviation under Attempt history.
- [ ] 5.A Act: If all 7 PASS, the change is ready — update the CHANGELOG ("Graphos agent/skill now explicitly declares the `graphos` CLI as its toolset; removed stale `explore_graphos.md` that referenced unregistered MCP tools; renamed the global skill from `graphify` to `graphos` (trigger `/graphos`); warns against the unrelated `graphify` Python tool"). If any FAIL, do NOT mark done — start attempt 2 targeting the failing criterion. If the build gate is blocked by the pre-existing `Extract/Core.hs:155` error, note it explicitly and ask the user whether to fix it inline.

### Attempt history (5)
<!-- empty unless retry needed -->
# 4.A Act: Implement the `goal-worker` subagent body

## Verdict
PASS

## Summary
The `goal-worker` body now covers all seven required criteria: plan-first with sign-off, ask-orchestrator on ambiguity, implement + test + edge cases, implementation report, `uv`/`pnpm` mandates, standalone (no other subagent dependencies), and tiered-rules + conflict resolution.

## Next step
Proceed to Task 5: validate the change and confirm OpenCode can load the new agents/command.

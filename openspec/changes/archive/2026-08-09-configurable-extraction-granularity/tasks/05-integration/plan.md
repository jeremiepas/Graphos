# Plan: Integration verification + CHANGELOG

## Goal
Prove the levels work end-to-end on a real run and document the default change.

## Check Criteria
function ≪ fine node counts on tree-sitter files; JSON 1-node; audit passes; fine matches old behavior; CHANGELOG entry; build + suite green.

## 1. Add `install-skill` CLI parser and wire to program entrypoint

- [x] 1.P Plan: Define a new `install-skill` subcommand with required `--target <TARGET>` option. Target is a closed enum (`opencode` only for now). Check criteria: (a) parser help lists `install-skill` and `--target`; (b) missing/invalid `--target` is rejected by the parser; (c) the subcommand is routed to a new handler function in `Program.hs` (or equivalent entrypoint) without breaking existing commands.
- [x] 1.D Do: Add `InstallSkillTarget` enum, `install-skill` parser branch, and route to `runInstallSkill` handler in CLI/Program. Keep changes minimal and consistent with existing parser style.
- [x] 1.C Check: Run `cabal build` and `cabal test`; verify `graphos --help` shows `install-skill`; verify `graphos install-skill` (no target) exits with usage error; verify `graphos install-skill --target foo` exits with error naming valid targets.
- [x] 1.A Act: Parser help text is clear and follows existing conventions. No additional documentation needed.

### Attempt history (1)

## 2. Extend scaffold UseCase with global opencode skill planning

- [x] 2.P Plan: Add a pure `installSkillPlan :: InstallSkillRequest -> CommandReference -> [FilePlan]` in the UseCase/Scaffold module. For `OpencodeTarget`, it returns exactly two file plans: `~/.agents/skills/graphos/SKILL.md` (full skill) and `~/.agents/skills/graphos-query/SKILL.md` (query-only skill). Check criteria: (a) plan is pure and deterministic; (b) paths are relative to a configurable global skills root; (c) both plans contain YAML frontmatter and the version stamp; (d) query-only plan forbids maintenance in its prose.
- [x] 2.D Do: Introduce `InstallSkillRequest` type, global skill templates (`opencodeGlobalSkillTemplate`, `opencodeGlobalQuerySkillTemplate`), and a shared frontmatter helper. Ensure templates reuse existing command-reference rendering.
- [x] 2.C Check: Unit test the planner with a fixed `CommandReference`; assert file count, relative paths, frontmatter presence, version stamp presence, and query-only prohibition text. Run `cabal test`.
- [x] 2.A Act: Planner output matches spec. Global templates use shared frontmatter helper while project-local templates remain untouched.

### Attempt history (2)

## 3. Update project-local skill template with YAML frontmatter

- [x] 3.P Plan: Modify the existing project-local `opencodeSkillTemplate` to prepend the same YAML frontmatter block before the version stamp. Check criteria: (a) generated `.opencode/skills/graphos/SKILL.md` begins with `---\nname: graphos\ndescription: ...\n---\n` followed by `generated by graphos ...`; (b) no other project-local behavior changes; (c) existing golden-file tests fail predictably and are updated in task 5.
- [x] 3.D Do: Refactor `opencodeSkillTemplate` to call the shared `skillFrontmatter` helper and then the existing version stamp and body.
- [x] 3.C Check: `cabal build` and `cabal test` pass. Project-local skill output begins with YAML frontmatter and version stamp.
- [x] 3.A Act: Template helper produces correct frontmatter for both global and project-local skills. No unintended changes detected.

### Attempt history (3)

## 4. Add Infrastructure writer for global install with idempotency

- [x] 4.P Plan: Implement `runInstallSkill` in Infrastructure. It resolves the global skills root to `getHomeDirectory </> ".agents" </> "skills"`, creates parent directories, writes each planned file if absent, skips existing files with a message, and exits 0 unless a filesystem error occurs. Check criteria: (a) writing to a temporary directory produces both files; (b) second run skips both; (c) one existing file is skipped while the other is written; (d) no files outside the temporary root are touched when root is injected.
- [x] 4.D Do: Add writer function reusing existing scaffold write/skip logic. Accept an output-root override for testability. Keep all IO in Infrastructure.
- [x] 4.C Check: Added infrastructure-level tests using temporary directory as global root. Verified idempotency and partial-skip behavior.
- [x] 4.A Act: Skip messages are clear and consistent. Writer never overwrites existing files. Idempotency verified via smoke test.

### Attempt history (4)

## 5. Update golden files and add global-install golden tests

- [x] 5.P Plan: Regenerate/update golden fixture files for project-local opencode skill (now with frontmatter). Add new golden files for global `graphos` and `graphos-query` skills. Add golden tests covering `install-skill --target opencode` output and idempotency. Check criteria: (a) all golden tests pass after regeneration; (b) new global golden files match UseCase planner output; (c) `cabal test` green.
- [x] 5.D Do: Created `tests/fixtures/scaffold/graphos-global-skill.md` and `graphos-query-global-skill.md` from planner output. Wired planner and writer tests into `UseCase.ScaffoldSpec` and `Infrastructure.Scaffold.WriterSpec`.
- [x] 5.C Check: `cabal test` green (342 examples). Golden files match planner output.
- [x] 5.A Act: No source bugs revealed; golden files stable.

### Attempt history (5)

## 6. End-to-end validation and PDCA close-out

- [x] 6.P Plan: Validate the full change with `cabal build --flag dev` (zero warnings), `cabal test`, and a manual smoke test that generated files start with correct frontmatter and version stamp. Check criteria: (a) build zero warnings; (b) all tests pass; (c) manual generated-file inspection matches spec scenario; (d) idempotency smoke test exits 0 and skips existing files.
- [x] 6.D Do: Ran `cabal build`, `cabal test`, and manual smoke tests with isolated HOME.
- [x] 6.C Check: (a) `cabal build` zero warnings; (b) `cabal test` passes (342 examples); (c) global skills begin with YAML frontmatter and version stamp; (d) idempotency smoke test exits 0 and skips existing files.
- [x] 6.A Act: All criteria pass. Change closed.

### Attempt history (6)

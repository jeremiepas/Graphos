# Task 6 — End-to-end validation and PDCA close-out — CHECK

**Task slug**: `06-e2e-validation`
**Attempt**: 1
**Status**: pending

## Summary

Run the full build/test suite and a safe manual smoke test, then record results for the criteria in `plan.md`.

## Detail

### Check Criteria (from plan.md)
- C1: `cabal build --flag dev` exits 0 with zero warnings.
- C2: `cabal test` exits 0.
- C3: Generated skill files start with `---\nname: ...\ndescription: ...\n---\n` followed by `generated by graphos ...`.
- C4: Second invocation skips existing files and exits 0.
- C5: `graphos-query` skill body forbids build/update/ingest.

### Execution
Pending implementation (`/opsx-apply`). Commands:
```bash
cabal build --flag dev
cabal test
# smoke test with isolated HOME directory
```

### Evidence
_TBD after implementation._

## Result

Pending `/opsx-apply`. Once executed, record PASS/FAIL per criterion with outputs.

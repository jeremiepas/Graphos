# Tasks: init-agent-scaffolding

> Sequencing note: this change lands AFTER `improve-query-agent-ergonomics`. Task 4 (template prose) depends on that change's final query CLI contract. Tasks 1–3 are parallel-safe foundations.

## 1. Domain scaffold types + pure template substitution

- [x] 1.P Plan: define `Domain.Scaffold` with `ScaffoldTarget` (Opencode/Claude/Generic), `ScaffoldFile` (relative path + content), `ScaffoldRequest`, and pure placeholder substitution (`{{VERSION}}`, `{{COMMAND_REFERENCE}}`, `{{GENERATED_STAMP}}`). Check criteria: (a) module has zero IO imports; (b) Hspec unit tests cover substitution incl. missing/duplicate placeholders; (c) `cabal build` clean with `-Wall -Werror`; (d) `cabal test` passes.
- [x] 1.D Do: implement types + substitution with explicit exports and type signatures; newtypes where useful.
- [x] 1.C Check: run criteria (a)–(d) verbatim; record PASS/FAIL per criterion.
- [x] 1.A Act: standardize substitution helper naming for reuse; note any template-format learnings for task 4.

### Attempt history (1)

## 2. Target selection: pure detection decision

- [x] 2.P Plan: pure function `selectTargets :: Maybe [ScaffoldTarget] -> DetectionFacts -> NonEmpty ScaffoldTarget` in UseCase/Domain implementing: explicit list wins; bare `--agents` uses facts (`.opencode/` exists → opencode, `.claude/` exists → claude); nothing detected → all three. Check criteria: (a) property/unit tests for all branches of the spec's "Target auto-detection" scenarios; (b) no IO imports; (c) `cabal test` passes.
- [x] 2.D Do: implement `DetectionFacts` record + `selectTargets`; wire nothing to IO yet.
- [x] 2.C Check: execute criteria (a)–(c); record results.
- [x] 2.A Act: confirm the facts-in/plan-out pattern reads well; feed naming into task 5 writer.

### Attempt history (2)

## 3. Parser extraction + command-reference rendering

- [x] 3.P Plan: move the optparse-applicative `Parser`/command definitions out of `app/Main.hs` into a shared module usable by both Main and scaffold rendering; implement `renderCommandReference` producing a compact usage block (top-level + query/path/explain/ingest/init subcommands, < ~60 lines). Check criteria: (a) `cabal build` clean, `graphos --help` output unchanged (byte-compare before/after); (b) Hspec consistency test: every `--flag`/subcommand token in rendered reference exists in real parser help; (c) reference block ≤ 60 lines; (d) `cabal test` passes.
- [x] 3.D Do: extract parser module; implement rendering via optparse-applicative help facilities; add consistency test.
- [x] 3.C Check: run criteria (a)–(d) verbatim.
- [x] 3.A Act: document the parser-module location for the architecture refactor change; note any help-rendering quirks.

### Attempt history (3)

## 4. Templates: skill + sub-agent for all three targets (post-ergonomics prose)

- [x] 4.P Plan: author embedded markdown templates (via `file-embed`) for: opencode SKILL.md, opencode sub-agent, claude SKILL.md, claude sub-agent, generic `.agents/graphos.md`. Prose written against the post-`improve-query-agent-ergonomics` query CLI. Sub-agent frontmatter: bash allowed for `graphos *` only, write/edit denied; body permits build/`--update`/`ingest`, forbids source edits, prefers `--update`/`--no-viz`. All files start with `generated by graphos {{VERSION}}` stamp. Check criteria: (a) golden tests: `planScaffold` output per target matches checked-in goldens; (b) frontmatter of opencode/claude files parses as valid YAML; (c) sub-agent spec scenarios ("restricts tools", "permits graph maintenance") verifiable in goldens; (d) stamp present as first lines; (e) `cabal test` passes.
- [x] 4.D Do: write templates + `planScaffold :: ScaffoldRequest -> CommandReference -> [ScaffoldFile]` in UseCase; add golden tests.
- [x] 4.C Check: run criteria (a)–(e) verbatim; verify `improve-query-agent-ergonomics` is archived/landed before finalizing prose — if not, record FAIL and hold.
- [x] 4.A Act: standardize template placeholder conventions; capture any opencode `.opencode/agent/` vs `agents/` path finding (design open question) in design.md.

### Attempt history (4)

## 5. Infrastructure writer: idempotent file emission + reporting

- [x] 5.P Plan: `Infrastructure.Scaffold.Writer` gathers `DetectionFacts`, creates parent dirs, writes each `ScaffoldFile` only if absent, prints per-file created/skipped report, exits 0 regardless of skips. Check criteria: (a) temp-dir Hspec tests: fresh dir writes all files; partial pre-existing scaffold skips existing byte-identically and still writes the rest; (b) exit behavior success in both cases; (c) all IO confined to Infrastructure (no new IO in Domain/UseCase); (d) `cabal test` passes.
- [x] 5.D Do: implement writer + report formatting; wire to `planScaffold`.
- [x] 5.C Check: run criteria (a)–(d) verbatim.
- [x] 5.A Act: confirm skip-message wording matches `graphos.yaml` init tone; standardize for future `agents update`.

### Attempt history (5)

## 6. CLI wiring: `graphos init --agents [targets]` + hint

- [x] 6.P Plan: extend `init` command with optional `--agents` taking optional comma-separated target list; invalid target → error naming it + listing valid targets, nothing written; plain `init` unchanged but prints one-line `--agents` hint. Check criteria: (a) `graphos init` in scratch dir creates only graphos.yaml + prints hint; (b) `graphos init --agents opencode` creates exactly the two opencode files; (c) `graphos init --agents claude,generic` creates the three expected files and no opencode files; (d) `graphos init --agents foo` errors, writes nothing; (e) `cabal build` clean + `cabal test` passes.
- [x] 6.D Do: add optparse parsing (in the extracted parser module), thread through to writer.
- [x] 6.C Check: execute criteria (a)–(e) verbatim in a temp project.
- [x] 6.A Act: reconcile help text with rendered command reference (it now documents itself — verify task 3 test still passes).

### Attempt history (6)

## 7. End-to-end acceptance + docs

- [x] 7.P Plan: manual + doc closure. Check criteria: (a) in a scratch project, `graphos init --agents` with no `.opencode/`/`.claude/` writes all five files; (b) opencode lists the `graphos` skill and `graphos-navigator` sub-agent; sub-agent answers a question via `graphos query` and refreshes via `graphos . --update`; (c) Claude Code loads the skill; (d) PRD §13.1 table row + README init section updated; (e) full `cabal test` suite green.
- [x] 7.D Do: run acceptance in scratch project; update PRD.md §13.1 and README; add workflow doc entry for agent scaffolding.
- [x] 7.C Check: run criteria (a)–(e) verbatim; record per-criterion PASS/FAIL.
- [x] 7.A Act: record format-drift watchpoints (opencode/Claude frontmatter) and MCP-registration follow-up as candidate next PDCA cycle.

### Attempt history (7)

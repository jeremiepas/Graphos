# Plan: adjacency-based community bridges
Goal: bridge only community pairs with >=1 real inter-community edge, capped at maxCommunityBridges.
Check: adjacent pair bridged once; disconnected not bridged; empty when no adjacency; build + suite clean.

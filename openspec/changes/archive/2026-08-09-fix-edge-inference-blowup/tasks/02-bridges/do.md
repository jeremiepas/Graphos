# Do
- Candidates from `Map.keys (gEdges g)` mapped through nodeCommunityMap, normalized + Set-deduped (O(E log C)).
- `take maxCommunityBridges` (10000, haddock'd); notEdgeAlready preserved.
- Star-community fixtures in InferSpec (leaf-connected so centroid pair has no existing edge).

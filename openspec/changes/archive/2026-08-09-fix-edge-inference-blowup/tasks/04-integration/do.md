# Do
- Full pipeline at Normal density on this repo.
- Audit script component heuristic updated: bound = source-file count (per-file islanding signal),
  since honest doc islands are expected once fabricated mesh connectivity is removed.

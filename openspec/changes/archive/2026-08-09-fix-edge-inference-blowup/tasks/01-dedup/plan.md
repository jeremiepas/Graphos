# Plan: dedupOn helper + nubBy replacement
Goal: O(k log k) first-wins order-preserving dedup replacing O(k²) nubBy at all graph-scale sites.
Check: QuickCheck equivalence vs nubBy; order/first-wins unit cases; build + suite clean.

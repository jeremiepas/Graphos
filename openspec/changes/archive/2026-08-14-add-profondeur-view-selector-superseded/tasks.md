## 1. Add depth selector markup + CSS to htmlHeader

- [ ] 1.P Plan: Add a `<select id="depthSelector">` with options `Overview`, `Community`, `Full`, `Custom` to the header in `htmlHeader` (next to the search box), plus a hidden `<input id="neighborhoodHops" type="number" min="1" max="6" value="2">` shown only when `Custom` is selected. Add CSS for the selector and the neighborhood input matching the existing dark theme (`#252540` background, `#3a3a5e` border, `#7dd3fc` focus). Remove the `<button id="btnBack">` markup. Affected: `htmlHeader` in `src/Graphos/Infrastructure/Export/HTML.hs` (the header `<style>` block + the `<header>` markup). No JS yet. Check criteria: (1) generated `graph.html` contains `<select id="depthSelector">` with exactly four options `Overview`, `Community`, `Full`, `Custom` in that order; (2) it contains `<input id="neighborhoodHops" type="number" min="1" max="6" value="2">`; (3) no element with id `btnBack` exists in the file; (4) `cabal build` zero warnings; (5) `cabal test` green.
- [ ] 1.D Do: In `htmlHeader`, remove the `btnBack` button line from the `<header>` block; add the `<select id="depthSelector">` and `<input id="neighborhoodHops">` lines in the `.search-box` div after the search input; add the `.depth-selector` and `.neighborhood-input` CSS rules to the `<style>` block (hide `.neighborhood-input` by default, `display: inline-block` when `.active`).
- [ ] 1.C Check: Run `cabal run graphos -- example/` to regenerate `graphos-out/graph.html`. Grep the file: (1) `<select id="depthSelector">` present with four `<option>` values matching the spec order; (2) `<input id="neighborhoodHops"` present with `min="1" max="6" value="2"`; (3) `rg -c "btnBack" graphos-out/graph.html` returns 0; (4) `cabal build` zero warnings; (5) `cabal test` green. Record PASS/FAIL per criterion.
- [ ] 1.A Act: If all PASS, standardize the header-control CSS pattern for future selectors. If `btnBack` references survive (e.g., in CSS or JS strings), record under Attempt history and start attempt 2 to remove all references.

### Attempt history (1)
<!-- empty if first attempt passes -->

## 2. Refactor JS state machine to multi-depth dispatcher

- [ ] 2.P Plan: Replace the `currentPhase: 'overview' | 'drilldown'` two-phase state in `htmlBody` with `currentDepth: 'overview' | 'community' | 'full' | 'custom'`. Add a `switchDepth(level)` dispatcher that: (a) destroys any existing `vis.Network` instance and removes its canvas from `#graph`; (b) routes to per-depth render functions. Keep `expandCommunity` and `backToOverview` as internal helpers called by the dispatcher. Affected: `htmlBody` JS in `src/Graphos/Infrastructure/Export/HTML.hs`. No `graph.json` change. Check criteria: (1) generated `graph.html` contains `let currentDepth = 'overview';` and a `function switchDepth(level)` definition; (2) no `currentPhase` variable remains; (3) `switchDepth('overview')` routes to the overview render (equivalent to old `backToOverview`); (4) `switchDepth('community')` routes to `expandCommunity` with the last selected community (or community 0); (5) `cabal build` zero warnings; (6) `cabal test` green.
- [ ] 2.D Do: In `htmlBody`, rename `currentPhase` to `currentDepth` everywhere; add `function switchDepth(level) { destroyCurrentNetwork(); if (level === 'overview') renderOverview(); else if (level === 'community') renderCommunity(expandedCommunity || firstCommunityId()); else if (level === 'full') renderFull(); else if (level === 'custom') renderCustom(); }`; factor the existing overview-init code into `renderOverview()` and the drilldown code into `renderCommunity(cid)`; add a `destroyCurrentNetwork()` helper that destroys whichever of `overviewNetwork`/`drilldownNetwork` is non-null and removes its canvas. Leave `renderFull` and `renderCustom` as stubs for tasks 3 and 4.
- [ ] 2.C Check: Regenerate `graph.html`; grep: (1) `rg "currentDepth" graphos-out/graph.html` finds the declaration and uses; (2) `rg "currentPhase" graphos-out/graph.html` returns 0; (3) `rg "function switchDepth" graphos-out/graph.html` finds the definition; (4) open in browser (or read the JS) — `switchDepth('overview')` calls `renderOverview`; `switchDepth('community')` calls `renderCommunity`; (5) `cabal build` zero warnings; (6) `cabal test` green. Record PASS/FAIL per criterion.
- [ ] 2.A Act: If PASS, the dispatcher is the single depth-navigation entry point. If `currentPhase` references survive in JS strings, record and start attempt 2.

### Attempt history (2)
<!-- empty unless retry needed -->

## 3. Implement Full depth render

- [ ] 3.P Plan: Implement `renderFull()` in `htmlBody` that renders all `allNodes` as individual dots (one per node, colored by community, sized 12, with labels) and all `allEdges` as edges, using a `barnesHut` physics solver that stabilizes then disables. This is the opt-in "show me everything" depth. Affected: `htmlBody` JS. No new data. Check criteria: (1) `switchDepth('full')` on the example graph (< 1K nodes) renders one dot per node within 1s (manual verification); (2) the rendered node count equals `allNodes.length`; (3) `cabal build` zero warnings; (4) `cabal test` green.
- [ ] 3.D Do: Implement `renderFull()`: build a `vis.DataSet` from `allNodes` (preserve `community_id`, `color`, `label`), build an edge `DataSet` from `allEdges`, create a new `vis.Network` with `barnesHut` options, disable physics after stabilization, wire `click` to `showNodeDetail`. Call `destroyCurrentNetwork()` first.
- [ ] 3.C Check: Regenerate `graph.html`; serve via `graphos serve --dir graphos-out --port 8080`; in browser: (1) select `Full` from the depth selector — all nodes render within ~1s on the example graph (no freeze); (2) count rendered dots == `allNodes.length` (verify via devtools: `overviewNetwork`/`drilldownNetwork` are null, the full network body has N node entries); (3) `cabal build` zero warnings; (4) `cabal test` green. Record PASS/FAIL.
- [ ] 3.A Act: If the example graph renders fast but a larger graph (> 5K) freezes, document the threshold and add a legend tooltip "Full view recommended for < 5K nodes" (design Decision 5). If freeze happens below 1K, record under Attempt history and start attempt 2 (e.g., add `hideEdgesOnDrag`).

### Attempt history (3)
<!-- empty unless retry needed -->

## 4. Implement Custom (neighborhood) depth with client-side BFS

- [ ] 4.P Plan: Implement `renderCustom(nodeId, hops)` and `buildNeighborhoodData(nodeId, hops)` in `htmlBody`. Build a `nodeAdj` map (nodeId → [neighborIds]) once on load from `allEdges`. `buildNeighborhoodData` performs BFS to `hops` levels, returns the induced subgraph (nodes within N hops + edges among them). The neighborhood input `#neighborhoodHops` is shown when `Custom` is selected; clicking a node in Custom depth re-runs BFS from that node. Affected: `htmlBody` JS. No `graph.json` change. Check criteria: (1) selecting `Custom` makes `#neighborhoodHops` visible; (2) clicking a known node `X` with hops `2` renders a node set equal to the output of `cabal run graphos -- neighbors X --depth 2` (cross-check the node count and a sample node id); (3) changing the input to `3` and re-clicking `X` renders the N=3 set (superset of N=2); (4) `cabal build` zero warnings; (5) `cabal test` green.
- [ ] 4.D Do: After `allEdges` is loaded, add `const nodeAdj = {}; allEdges.forEach(e => { (nodeAdj[e.from] ||= []).push(e.to); (nodeAdj[e.to] ||= []).push(e.from); });`. Implement `buildNeighborhoodData(nodeId, hops)` using a visited Set + frontier BFS to `hops`. Implement `renderCustom(nodeId, hops)` calling `destroyCurrentNetwork()`, building a `vis.DataSet` from the induced nodes/edges, rendering with `barnesHut`. Wire the `#depthSelector` change handler to toggle `.active` on `#neighborhoodHops` when `custom` is selected. Wire network `click` in Custom depth to call `renderCustom(clickedNodeId, currentHops)`. Wire `#neighborhoodHops` `change` to re-run `renderCustom` with the current focus node and new hop count.
- [ ] 4.C Check: Regenerate `graph.html`; serve; in browser: (1) select `Custom` — `#neighborhoodHops` becomes visible with value 2; (2) click a node `X` — compare the rendered node set to `cabal run graphos -- neighbors X --depth 2` output (node count must match; spot-check 3 node ids); (3) change input to 3, click `X` again — rendered set is a superset of the N=2 set; (4) `cabal build` zero warnings; (5) `cabal test` green. Record PASS/FAIL.
- [ ] 4.A Act: If the N=2 set differs from the CLI, the BFS or adjacency is wrong — record the diff under Attempt history and start attempt 2 (likely an edge-direction or dedup bug). If BFS is slow above N=4, cap `#neighborhoodHops` `max="4"` and add a tooltip (design Decision 5).

### Attempt history (4)
<!-- empty unless retry needed -->

## 5. Wire sessionStorage persistence + DOMContentLoaded restore

- [ ] 5.P Plan: Persist `currentDepth` to `sessionStorage` key `graphos_depth` on every `switchDepth` call; persist `graphos_neighborhood_node` and `graphos_neighborhood_hops` on Custom-depth changes. On `DOMContentLoaded`, read these keys and restore the depth (default `overview` if absent or invalid). Wrap `sessionStorage` writes in `try/catch` (private-mode safety). Affected: `htmlBody` JS. Check criteria: (1) selecting `Full`, then reloading (same tab), loads directly into `Full` depth; (2) selecting `Custom`, focusing node `X`, hops `3`, then reloading, restores `Custom` with hops `3` and re-renders `X`'s N=3 neighborhood; (3) a fresh session (cleared `sessionStorage`) loads into `Overview`; (4) `cabal build` zero warnings; (5) `cabal test` green.
- [ ] 5.D Do: Add `function persistDepth() { try { sessionStorage.setItem('graphos_depth', currentDepth); if (currentDepth === 'custom') { sessionStorage.setItem('graphos_neighborhood_node', focusNode || ''); sessionStorage.setItem('graphos_neighborhood_hops', String(currentHops)); } } catch(e) {} }` and call it at the end of `switchDepth`. In the `DOMContentLoaded` handler, before `renderCommunityList()`/`initOverview`, read `graphos_depth` and if it is one of the four valid values, call `switchDepth` with it (passing the restored `graphos_neighborhood_node`/`graphos_neighborhood_hops` for custom); otherwise default to `overview`.
- [ ] 5.C Check: Regenerate `graph.html`; serve; in browser: (1) select `Full`, reload — page loads into Full; (2) select `Custom`, click node `X`, set hops `3`, reload — page loads into Custom with hops 3 and renders `X`'s neighborhood; (3) open devtools, clear `sessionStorage`, reload — page loads into Overview; (4) `cabal build` zero warnings; (5) `cabal test` green. Record PASS/FAIL.
- [ ] 5.A Act: If restore fails on a specific browser (e.g., Safari private mode throws on `sessionStorage` access), the `try/catch` should default to `overview` — verify that path. If a key is stale (e.g., `graphos_neighborhood_node` points to a node no longer in the data), the restore must fall back to `overview` — record under Attempt history if not and start attempt 2.

### Attempt history (5)
<!-- empty unless retry needed -->

## 6. End-to-end acceptance pass against the spec scenarios

- [ ] 6.P Plan: Run the full manual acceptance checklist from `design.md` Verification Strategy against the regenerated `graph.html` on the `example/` graph, covering every scenario in `specs/html-depth-selector/spec.md` and the MODIFIED scenarios in `specs/html-lod-viewer/spec.md`. No new code unless a scenario fails. Check criteria (verbatim from the specs): (1) selector present on load, defaults to Overview; (2) switching to Full on < 1K-node graph renders all nodes within 1s; (3) switching to Community expands last-selected (or community 0); (4) Custom depth shows neighborhood input (min 1, max 6, value 2); (5) N=2 neighborhood matches `graphos neighbors <id> --depth 2`; (6) changing hops re-runs BFS; (7) reload preserves depth via `sessionStorage`; (8) no `btnBack` element in DOM; (9) Overview selectable from any depth; (10) `cabal build` zero warnings + `cabal test` green.
- [ ] 6.D Do: Regenerate `graph.html` via `cabal run graphos -- example/`. Serve via `graphos serve --dir graphos-out --port 8080`. Walk through each of the 10 criteria in a browser. For criterion 5, run the CLI `cabal run graphos -- neighbors <id> --depth 2` and diff the node set against the browser's Custom-depth render.
- [ ] 6.C Check: Record PASS/FAIL for each of the 10 criteria. Do not invent new criteria. If any FAIL, record the specific criterion and the observed deviation under Attempt history.
- [ ] 6.A Act: If all 10 PASS, the change is ready — update the CHANGELOG entry ("depth selector replaces back button; adds Full and Custom neighborhood views to graph.html"). If any FAIL, do NOT mark the change done — start attempt 2 targeting the failing criterion (fix in the relevant task above, then re-run this acceptance pass).

### Attempt history (6)
<!-- empty unless retry needed -->
## 1. Domain foundation — pure preparation (D8, D1, D3)

- [x] 1.1 Create `src/Graphos/Domain/Embedding.hs` with pure `prepare :: EmbeddingConfig -> Text -> Text` (docPrefix prepend + conservative char÷4 truncation), `effectiveTokenLimit :: EmbeddingConfig -> Int` (static model-limit table: nomic-embed-text 8192, LFM2.5-Embedding-350M 512, all-minilm 256, unknown → `embMaxTokens` or no truncation), and export an explicit interface list. Verify: `cabal build` clean under `-Wall -Werror` dev flags.
- [x] 1.2 Add `embMaxTokens`, `embDocPrefix`, `embQueryPrefix` fields to `EmbeddingConfig` (`Domain/Config/Vision.hs` block) with FromJSON defaults 0 / empty / empty (0 = model-default limit, empty prefix = passthrough). Extend `validateEmbeddingConfig` to reject `maxTokens < 0` naming `embedding.maxTokens`. Verify: extend `ConfigSpec` — defaults when keys absent (behavior identical to today), explicit values parsed, negative rejected with key name.
- [x] 1.3 Write `Domain/Embedding` unit tests (new `EmbeddingSpec`): passthrough for short texts byte-identical (no prefix, under limit), `docPrefix` prepended when configured, over-limit text truncated to ≤ `effectiveTokenLimit` tokens under char÷4 estimate, `maxTokens` override wins over model-table limit, unknown model without `maxTokens` returns text unchanged. Verify: `cabal test EmbeddingSpec` green.

## 2. Infrastructure — prepared transport + per-input isolation (D2, D4)

- [x] 2.1 In `LLM/Embedding.hs`, apply `prepare` to every input text before building the JSON payload; the payload and response parsing are otherwise unchanged. Verify: extend the existing `parseEmbeddingsResponse`/payload tests to show prepared texts are what the API receives (fake manager or request-builder pure test asserting the encoded body contains the prepared string).
- [x] 2.2 Implement batch bisection on whole-request failure in the transport layer (or in the pipelines' `embedChunk` boundary per design D2): on failure with >1 texts, retry halves independently; at batch size 1, log the failing text (first 200 chars) and its error, return per-input failure for that text only. Verify: `PipelineSpec` new case — a 64-text batch containing one poison text yields vectors for the 63 siblings (the existing "failing text lands in its own batch" test is the size-1 model; add a size-8 case asserting the sibling vectors survive).
- [x] 2.3 Extend `EmbeddingCache.cacheKey` to `SHA256(model <> docPrefix <> preparedText)` and `embeddingSourceHash` identically (hash what was sent, D4). Verify: `IngestSpec` — same raw text + changed prefix ⇒ different hash; two raw texts differing only past the truncation point ⇒ equal hash (convergence).
- [x] 2.4 Add cache-hit read-only behavior to `saveVector` callers: pipelines write only fresh (miss) vectors, never hits (D5). Verify: `PipelineSpec` new case — a fully warm second run performs zero `saveVector` writes (record writes in a fake or count files in a temp cache dir).

## 3. UseCase — pipeline integration

- [x] 3.1 Confirm `Pipeline/Core.hs generateGraphEmbeddings` and `Ingest.hs generateEmbeddingsForNodes` pass raw texts unchanged and receive prepared-text behavior transparently (dedup over raw texts, cache convergence beneath, D8). Verify: `cabal test PipelineSpec IngestSpec` green after group 2; no signature changes in either function.
- [x] 3.2 Add the concurrency-sweep gate wiring: effective concurrency remains `embConcurrency` (no code change expected); update the pipeline debug log line to include the prepared/cached/missed counts as today. Verify: manual `--embed` run on a small corpus prints the log line with correct counts.
- [x] 3.3 Add `embStreaming :: Bool` to `EmbeddingConfig` with FromJSON default `true` (`streaming` key). Verify: extend `ConfigSpec` — default `true` when key absent, explicit `false` parsed.
- [x] 3.4 Implement the streaming sidecar write in `generateGraphEmbeddings` (D9): with `streaming: true`, open a staged `embeddings.json.staging` file in the output directory, append each completed batch's `(NodeId, vector)` entries as batches complete, atomically rename into `embeddings.json` on pass completion; with `false`, keep the current `writeEmbeddingsSidecar` write-at-end. An aborted pass MUST NOT rename (prior sidecar intact; staged file left or removed per `withStagedOutput` convention). Verify: `PipelineSpec` new cases — (a) completed streaming run's sidecar content equals write-at-end content for the same inputs; (b) simulated abort (exception mid-pass) leaves the previous `embeddings.json` untouched and no partial file at the final path.

## 4. Benchmark harness + recorded results (D7, D6)

- [x] 4.1 Write `bench/bench.py` under the change directory: node-shaped synthetic corpus (`<label> <sourcefile>` patterns sized from the repo's label distribution), POSTs `/v1/embeddings` to a configurable base URL/model, sweeps concurrency {1,2,4,8} at batchSize 64, reports texts/s, wall-clock, hardware (cpu count, RAM) as a Markdown table. Verify: `python3 bench/bench.py --help` runs; a 100-text smoke run against the local server completes.
- [x] 4.2 Run the sweep on an idle machine against LFM2.5 (llama-server) and nomic (Ollama); record results, date, hardware, and server config in `bench/BENCHMARK.md`; pin the default concurrency (lowest level ≥90% of plateau, D6) and the oversized-input probe result (one >512-token text accepted after truncation, siblings intact). Verify: `BENCHMARK.md` contains dated rows for both models and the pinned value with the citation.
- [x] 4.3 Add the oversized-input regression probe to the bench harness: one over-limit text in a 64-text batch must not lose siblings' vectors against the real server (per spec "Oversized text does not lose siblings' vectors"). Verify: probe output line in `BENCHMARK.md` showing sibling count 63/63.

## 5. Lean 4 verification artifact (D1, D5; lean-proof-methodology rules)

- [x] 5.1 Create `lean/lakefile.toml` (no Mathlib) and `lean/EmbedPerf.lean` extending the archived `EmbedPipeline.lean` master theorems: (a) instantiation of `pipeline_eq_baseline` at `f ∘ prepare` for the preparation transform; (b) misses-only write-back lemma — a run that reads sound entries and writes only fresh ones leaves the cache sound and the assignment equal; (c) prefix-composition lemma — `prepare = (· <> prepared) ∘ truncate` form composes with the existing key-invariance argument; (d) streaming-write lemma — staged per-batch append then rename produces sidecar content equal to write-at-end (`concat (map entriesOf batches)` = flattening associativity over the existing `chunks_flatten` pattern). Follow methodology: goal-guided invocation of `runCached_fst`/`runCached_sound` (never positional cache args), `rcases hbeq : (a == k)` for lookup goals, no `by decide` on lookup-bearing statements, `set_option linter.unusedSectionVars false`. Verify: `lake clean && lake build` with pinned Lean 4.29.1 exits 0, zero errors/warnings.
- [x] 5.2 Record the toolchain provenance (`nix run nixpkgs#lean4`, exact store path, warning that `nixpkgs#lean` is Lean 3), exact commands, and the green run log in `lean/VERIFICATION.md`. Verify: a second `lake clean && lake build` from a fresh shell reproduces exit 0; log entry dated.

## 6. Defaults, config surface, docs (D6, spec embedding/Default model)

- [x] 6.1 Flip `embedding.model` default to the LFM2.5-Embedding-350M GGUF reference (`hf.co/LiquidAI/LFM2.5-Embedding-350M-GGUF:Q4_K_M`), set `embedding.concurrency` default to the 4.2-pinned value, and add `maxTokens`/`docPrefix`/`queryPrefix`/`streaming` documented keys in `graphos.yaml` comments + `app/Main.hs` config-init template. Verify: `ConfigSpec` defaults test updated to the new model/concurrency; config-init output shows the new keys with correct comment text.
- [x] 6.2 Update `docs/embedding-models.md`: add the LFM2.5-Embedding-350M row (512-token doc limit, 1024-dim, asymmetric `document:`/`query:` prefixes, ~343 req/s measured on llama-server), a "Token limits and preparation" section pointing to `maxTokens`, and a pointer to the recorded benchmark. Verify: doc renders (markdown lint if configured) and cites `BENCHMARK.md`.
- [x] 6.3 End-to-end verification run: `graphos . --embed` on a small mixed corpus (docs + code) with the LFM default — zero HTTP 500s, embeddings sidecar written with 1024-dim vectors (via the streaming staged path), cache populated, second run warm (zero API calls, zero cache writes, no re-name churn). Verify: run log inspected; `embeddings.json` dimension spot-checked; cache file count stable across the second run; no staging leftovers in the output directory.
- [x] 6.4 Full regression gate before hand-off: `cabal build --flag dev` and `cabal test` fully green, including the extended `PipelineSpec`/`IngestSpec`/`ConfigSpec`/`EmbeddingSpec`. Verify: zero failures; record the summary in the change directory (`E2E_LOG.md` style note with date and command outputs).
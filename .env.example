# Graphos Development Environment
# Copy this file to .env and update values for your setup.
# NEVER commit .env to version control.

# Neo4j authentication (format: username/password)
# Use "none" to disable auth (dev only).
NEO4J_AUTH=neo4j/graphos_dev
